/**
 * Tipos de anexos suportados pelo sistema
 */
export enum AttachmentType {
  PHOTO_BEFORE = 'photo_before', // Foto antes do serviço
  PHOTO_DURING = 'photo_during', // Foto durante o serviço
  PHOTO_AFTER = 'photo_after', // Foto após o serviço
  DOCUMENT = 'document', // Documento (PDF, etc.)
}
